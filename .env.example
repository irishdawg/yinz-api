ENVIRONMENT=development

# Not consumed yet — the domain engine has zero I/O and doesn't need these.
# Wired in once persistence moves off the in-memory repository.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=
